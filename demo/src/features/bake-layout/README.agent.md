# Bake Layout Feature

Owns demo-only bake/load helpers extracted from `demo/src/main.ts`.

Keep persistent graph state in `main.ts`; this feature should use facades for graph construction, visual attributes, and current-data assignment.
