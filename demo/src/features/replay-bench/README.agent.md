# Replay Bench Feature

Owns demo-only benchmark and deterministic replay capture helpers extracted from `demo/src/main.ts`.

Keep this feature focused on browser-side capture mechanics: timing statistics, replay samples, upload/download fallback behavior, and small runtime facades back into the live graph owned by `main.ts`.
